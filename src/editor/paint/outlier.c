#include"paint.h"

/*
I will finish this folder after submit fdf42 project.

The purpose of this folder is to allow user to color every cells
with height/rgba channel below or above 80% of other every cells
accoding to normal distribution statistic of every cells,
with in O(n) time.
*/

// time : O(n)
// space: O(1)
void	print_outlier(void)
{
	write(1, "editor/paint/outlier.c\n", 24);
}
