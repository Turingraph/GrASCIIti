#include"view.h"

// time : O(n)
// space: O(n)
int	view_table_fdf(const t_table_fdf *src)
{
	mlx_image_t	dst;

	if (src == NULL || src->arr == NULL
		|| src->row == 0 || src->col == 0)
		return (-1);
	dst = mlx_new_image()
	return (1);
}

